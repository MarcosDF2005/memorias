services:
  - type: web
    name: memoria-digital
    runtime: node
    buildCommand: npm install && npm run setup-users
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
